# vpn-checker-api
REST API built with Flask that checks if an IP address is behind a VPN, proxy or TOR node. Aggregates results from ip-api.com, proxycheck.io and iphub.info into a single risk score.  
